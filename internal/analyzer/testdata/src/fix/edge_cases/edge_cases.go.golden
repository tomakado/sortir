package edge_cases

// Test single var declarations outside groups (no sorting expected)
var z = "z"
var y = "y"

// Test empty struct
type EmptyStruct struct{}

// Test single field struct (no sorting expected)
type SingleField struct {
	Field int
}

// Test already sorted
var (
	a = 1
	b = 2
	c = 3
)